# wercker.yml
box: alpine
build:
 steps:
   - script:
     name: prepare output
     code: rm -rf $WERCKER_ROOT/.git

publish:
 box: debian:stable-slim
 steps:
   - install-packages:
     packages: ca-certificates
   - internal/publish-step:
     private: true
